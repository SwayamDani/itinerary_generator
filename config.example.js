// This is an example file that shows the structure of your config
// without containing any actual secrets.

const config = {
    OPENAI_API_KEY: 'your-api-key-here' // Replace this with process.env.OPENAI_API_KEY in the real config.js
  };
  
  export default config;